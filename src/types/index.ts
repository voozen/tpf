export type { AppData } from './appData';
export type { Expense } from './expense';
export type { Group } from './group';

export type User = {
  id: string;
  name: string;
  email: string;
  initials: string;
};
